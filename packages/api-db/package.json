{
  "name": "@chipper/api-db",
  "version": "1.0.0",
  "description": "Exposes the DB version",
  "main": "index.ts",
  "scripts": {
    "build": "tsc",
    "migrate": "NODE_PATH=build node dist/migrate.js",
    "test": "jest",
    "start": "NODE_PATH=build node dist/index.js"
  },
  "dependencies": {
    "koa": "^2.13.0",
    "pg": "^8.5.1"
  },
  "devDependencies": {
    "@types/jest": "^26.0.18",
    "jest": "^26.6.3"
  }
}
